<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.photo.judge.dao.PhotoUserMapperDao">

    <select id="findUserById" resultType="com.photo.judge.entity.user.PhotoUser">
        SELECT user_id as userId, user_name as userName, password as password
        FROM photo_user WHERE user_id = #{id,jdbcType=VARCHAR}
    </select>

    <select id="findUserByUserName" resultType="com.photo.judge.entity.user.PhotoUser">
        SELECT user_id as userId, user_name as userName, password as password
        FROM photo_user WHERE user_name = #{userName,jdbcType=VARCHAR}
    </select>

    <select id="selectUserNames" resultType="java.lang.String">
        SELECT user_name as userName
        FROM photo_user
    </select>

</mapper>